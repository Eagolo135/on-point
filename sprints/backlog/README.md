# Sprint Backlog

Use this folder for approved sprint plans that are not yet active.

Rules:
1. Every sprint in backlog must map to one or more intake IDs.
2. Every backlog sprint must reference affected package specs.
3. Move a sprint to `sprints/current` only when work starts.
